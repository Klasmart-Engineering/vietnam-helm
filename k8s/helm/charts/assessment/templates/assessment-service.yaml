apiVersion: v1
kind: Service
metadata:
  name: {{ include "assessment.fullname" . }}-svc
  labels:
    {{- include "assessment.labels" . | nindent 4 }}
  {{- if eq .Values.provider "gcp"}}
  annotations:
    cloud.google.com/backend-config: '{"default": "assessment"}'
  {{- end}}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    {{- include "assessment.selectorLabels" . | nindent 4 }}
  type: {{ .Values.serviceType }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "kl2.fullname" . }}-svc
  labels:
    {{- include "kl2.labels" . | nindent 4 }}
  {{- if eq .Values.provider "gcp"}}
  annotations:
    cloud.google.com/backend-config: '{"default": "kl2"}'
  {{- end}}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8088
  selector:
    {{- include "kl2.selectorLabels" . | nindent 4 }}
  type: {{ .Values.serviceType }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "auth-frontend.fullname" . }}
  labels:
    {{- include "auth-frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "auth-frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "auth-frontend.selectorLabels" . | nindent 8 }}
        redis-client: "true"
    spec:
      nodeSelector:
        {{- toYaml .Values.nodegroupSelector | nindent 12 }}
      {{- if .Values.hostNetworking }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: "{{ .Values.image.pullPolicy }}"
        ports:
          - name: http
            protocol: TCP
            containerPort: 80
        resources:
          {{- toYaml .Values.resources | nindent 12 }}


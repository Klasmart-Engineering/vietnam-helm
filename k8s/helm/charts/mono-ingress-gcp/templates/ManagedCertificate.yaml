apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: mono-ingress
spec:
  domains:
    - hub{{ .Values.domain }}
    - cms{{ .Values.domain }}
    - live{{ .Values.domain }}  
    - api{{ .Values.domain }} 
    - auth{{ .Values.domain }}
    #- h5p{{ .Values.domain }}

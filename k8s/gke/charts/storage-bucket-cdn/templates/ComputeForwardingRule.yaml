apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputeForwardingRule
metadata:
  annotations:
    cnrm.cloud.google.com/project-id : {{ .Values.gcp.project }}
  name: {{ .Values.bucket_id }}
spec:
  ipAddress:
    addressRef:
      name: {{ .Values.bucket_id }}
  location: global
  target: 
    targetHTTPProxyRef:
      name: {{ .Values.bucket_id }}
  ports: 
    - "80"
  